import { defineConfig } from "astro/config";

// site/base normalde her firma projesi oluşturulurken doldurulur
// (GitHub Pages: https://<user>.github.io/<repo>/)
export default defineConfig({
  site: "https://ylmzfurkan.github.io",
  base: "/revize-siteler/balikci-kenan",
});
